# Version properties for Podcast 2.0 AI Player
# This file is automatically updated during CI/CD release builds
# Manual edits will be overwritten

# App version (semantic versioning)
version.name=1.0.0

# Build number (incremented by CI)
version.code=1

# Build timestamp (ISO 8601 format)
build.time=development

# Git information
git.commit=local
git.branch=local

# Build type
build.type=development
