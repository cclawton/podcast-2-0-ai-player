<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permission is OPTIONAL - app works offline -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Media playback permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Optional: Voice input for MCP commands -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Optional: Download to external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- Notification permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service for downloads -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Custom signature-level permission for MCP control -->
    <permission
        android:name="com.podcast.app.permission.MCP_CONTROL"
        android:protectionLevel="signature"
        android:label="MCP Control Permission"
        android:description="@string/mcp_permission_description" />

    <uses-permission android:name="com.podcast.app.permission.MCP_CONTROL" />

    <application
        android:name=".PodcastApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PodcastApp"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="35">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PodcastApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Media Playback Service -->
        <service
            android:name=".playback.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- MCP Socket Service (internal only) -->
        <service
            android:name=".mcp.service.MCPSocketService"
            android:exported="false" />

        <!-- MCP Broadcast Receiver (signature-protected) -->
        <receiver
            android:name=".mcp.service.MCPActionReceiver"
            android:exported="true"
            android:permission="com.podcast.app.permission.MCP_CONTROL">
            <intent-filter>
                <action android:name="com.podcast.app.action.MCP_COMMAND" />
            </intent-filter>
        </receiver>

        <!-- Disable default WorkManager initialization - using HiltWorkerFactory instead -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
